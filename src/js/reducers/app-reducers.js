import * as actions from '../actions' ;

const initialState = {
    category:-1,
    categoryProducts: [],
    user:null,

} ;

export default function (state = initialState, action) {
    // Todo: add reducing...
    return state ;

}